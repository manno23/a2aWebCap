{
  "name": "a2a-webcap-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run clean && npm run install:all && npm run build:packages",
    "install:all": "cd packages/shared && npm install && cd ../server && npm install && cd ../client && npm install && cd ../..",
    "build:packages": "cd packages/shared && npm run build && cd ../server && npm run build && cd ../client && npm run build && cd ../..",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules",
    "dev:server": "tsx packages/server/src/index.ts",
    "demo:capnweb": "tsx examples/acp-server-example/session.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.1.0",
    "@typescript-eslint/parser": "^7.1.0",
    "@vitest/coverage-v8": "^4.0.8",
    "@vitest/ui": "^4.0.8",
    "eslint": "^8.57.0",
    "tsx": "^4.20.6",
    "typescript": "^5.0.0",
    "vitest": "^4.0.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
